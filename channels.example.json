{
  "__comment__": "Reference only — DO NOT edit this file. Your real channels live in channels.json, which is created/managed for you by scripts/channels.py (add/login/remove). Each channel is a name mapped to its own cached OAuth token file, and optionally its own client_secret (for a separate Google Cloud project = its own upload quota) and per-channel overrides like privacy.",
  "main": {
    "token_file": "state/youtube_token.json"
  },
  "second": {
    "token_file": "state/youtube_token_second.json",
    "client_secret": "client_secret_second.json",
    "privacy": "unlisted"
  }
}
